@PACKAGE_INIT@

get_filename_component(DRAWBICYC_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

if(NOT TARGET DRAWBICYC)
    include("${DRAWBICYC_CMAKE_DIR}/drawbicyc-targets.cmake")
endif()
